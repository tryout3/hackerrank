#
# Complete the pageCount function below.
#
def pageCount(n, p):
    a = p//2 
    
    b = ((n)-p) // 2

    if n == 2:
        return 0
    
    if n - p == 1 and n % 2 == 0:
        return (int (1))
    
    else:
        return min(a,b)
    
   
